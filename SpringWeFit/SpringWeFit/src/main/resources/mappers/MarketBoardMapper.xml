<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.spring.wefit.market.mapper.IMarketBoardMapper">
 
 	<select id="getList" resultType="com.spring.wefit.command.marketBoardVO">
 		SELECT * FROM MarketBoard
 	</select>
 	
 	<insert id="regist">
 		INSERT INTO MarketBoard(mbNum,mNum, mbType, mbTitle, mbPrice, mbContent, mbImage1 )
 		values(marketBoard_seq.NEXTVAL,#{mNum},#{mbType}, #{mbTitle}, #{mbPrice}, #{mbContent}, #{mbImage1})
 	</insert>
 
 </mapper>